import React from "react";
import { IconType } from "react-icons";

interface Props {
    Icon: IconType;
    label: string;
    value: string;
}

const DataTile: React.FC<Props> = ({ Icon, label, value }) => {
    return <div></div>;
};

export default DataTile;
